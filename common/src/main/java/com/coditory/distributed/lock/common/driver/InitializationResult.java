package com.coditory.distributed.lock.common.driver;

public class InitializationResult {
  private boolean initialized;

  public InitializationResult(boolean initialized) {
    this.initialized = initialized;
  }
}
